import styled from "styled-components";

const EntityImageM = styled.img`
  display: inline;
  margin-right: 6px;
  width: ${props => props.theme.fontSizeM};
  height: ${props => props.theme.fontSizeM};
`;

export default EntityImageM;
